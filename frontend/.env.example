# Optional: call API directly (bypass Vite proxy), e.g. from another origin
# VITE_API_BASE_URL=http://localhost:3001

# Must match a real `users.id` in MySQL for createArticle / upload
# VITE_DEFAULT_USER_ID=1
