# Copy to ".env" in this same folder (backend/). Without DB_PASSWORD, MySQL error: "using password: NO"
PORT=3001
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=karen
DB_NAME=dhl_kb
CORS_ORIGIN=http://localhost:5173